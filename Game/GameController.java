import Observer.GameEventLogPublisher;

public class GameController {
    private GameEventLogPublisher publisher; // handles log generation
    // handle scoring
    // handle player states and / or actions (use state design pattern)
}
